┌─────────────────────────────────────────────────────────────────────────┐
│                           用户界面层 (View)                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │   3D场景    │  │   搜索框    │  │   导航面板  │  │   信息弹窗  │    │
│  │  (Three.js) │  │  (Vue组件)  │  │  (Vue组件)  │  │  (Vue组件)  │    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘    │
│         │                │                │                │            │
│         └────────────────┴────────────────┴────────────────┘            │
│                                   │                                     │
│                                   ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                        状态管理层 (Pinia Store)                   │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │    │
│  │  │ 场景Store │  │ 导航Store │  │ 用户Store │  │  UI Store │        │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                   │                                     │
│                                   ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                        服务层 (Services)                         │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │    │
│  │  │ 3D引擎服务│  │ API服务  │  │ 路径规划 │  │ 资源加载 │        │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                   │                                     │
│                                   ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                        路由层 (Vue Router)                       │    │
│  │     /home  /map  /building/:id  /route  /search                │    │
│  └─────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘

campus-navigation/
├── public/
│   └── models/              # 3D模型文件 (.glb)
│       ├── campus.glb
│       └── buildings/
│
├── src/
│   ├── assets/              # 静态资源
│   ├── components/          # Vue组件
│   │   ├── scene/          # 3D场景相关组件
│   │   │   ├── CampusScene.vue      # 主3D场景容器
│   │   │   ├── BuildingMarker.vue   # 建筑标记点
│   │   │   └── RouteLine.vue        # 导航路径线
│   │   ├── ui/             # UI组件
│   │   │   ├── SearchBar.vue        # 搜索框
│   │   │   ├── NavPanel.vue         # 导航面板
│   │   │   └── InfoModal.vue        # 信息弹窗
│   │   └── common/         # 通用组件
│   │
│   ├── composables/         # 组合式函数
│   │   ├── useThree.js     # Three.js核心逻辑
│   │   ├── useCamera.js    # 相机控制
│   │   └── useRaycaster.js # 射线检测
│   │
│   ├── stores/             # Pinia状态管理
│   │   ├── sceneStore.js   # 3D场景状态
│   │   ├── navStore.js     # 导航状态
│   │   ├── buildingStore.js# 建筑数据
│   │   └── uiStore.js      # UI状态
│   │
│   ├── services/           # 服务层
│   │   ├── threeService.js # Three.js引擎封装
│   │   ├── apiService.js   # API请求封装
│   │   └── pathfinding.js  # 路径规划算法
│   │
│   ├── router/             # 路由配置
│   │   └── index.js
│   │
│   ├── views/              # 页面视图
│   │   ├── HomeView.vue
│   │   ├── MapView.vue
│   │   └── BuildingView.vue
│   │
│   ├── utils/              # 工具函数
│   │   ├── coordinate.js   # 坐标转换
│   │   └── loader.js       # 资源加载
│   │
│   ├── App.vue
│   └── main.js
│
└── package.json